const mongoose=require('mongoose');
var newSchema={
    name:{
        type:String,
        trim:true,
        required:true
    },
    email:{
        type:String,
        trim:true,
        required:true
    },
    password:{
        type:String,
        trim:true,
        required:true
    },
}
module.exports=User=mongoose.model('integrow',newSchema)